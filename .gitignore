# Python bytecode
__pycache__/
*.pyc

# LaTeX build artifacts (scoped to paper/ so the logs/ dir is untouched).
paper/*.aux
paper/*.log
paper/*.out
paper/*.pdf
!paper/wagstaff_bls_primality.pdf
paper/*.synctex.gz
paper/*.fls
paper/*.fdb_latexmk
paper/*.toc
paper/*.bbl
paper/*.blg
paper/*.nav
paper/*.snm
paper/*.vrb

# Driver-side scratch copy of run logs. The canonical tracked location
# is logs/; drivers also write next to the JSON in data/ as a byproduct.
data/*.log

# Editor junk
*~
.DS_Store
.vscode/
